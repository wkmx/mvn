# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      - name: Login to ACR
        uses: aliyun/acr-login@v1
        with:
          login-server: https://registry.cn-hangzhou.aliyuncs.com
          username: ${{ secrets.ACR_USERNAME }}
          password:  ${{ secrets.ACR_PASSWORD }}

      - name: Push to Docker Registry
        run: |
      - name: Push to Docker Registry
        run: |
          # docker pull quay.io/minio/minio:RELEASE.2022-09-17T00-09-45Z
          REGISTRY="registry.cn-hangzhou.aliyuncs.com/wkmx"

          docker pull calico/cni:v3.23.2
          docker pull calico/kube-controllers:v3.23.2
          docker pull calico/node:v3.23.2
          docker pull calico/pod2daemon-flexvol:v3.23.2
          docker pull coredns/coredns:1.8.0
          docker pull kubesphere/k8s-dns-node-cache:1.15.12
          docker pull kubesphere/kube-apiserver:v1.21.5
          docker pull kubesphere/kube-controller-manager:v1.21.5
          docker pull kubesphere/kube-proxy:v1.21.5
          docker pull kubesphere/kube-scheduler:v1.21.5
          docker pull kubesphere/pause:3.4.1
          
          
          docker tag calico/cni:v3.23.2 ${REGISTRY}/cni:v3.23.2
          docker tag calico/kube-controllers:v3.23.2 ${REGISTRY}/kube-controllers:v3.23.2
          docker tag calico/node:v3.23.2 ${REGISTRY}/node:v3.23.2
          docker tag calico/pod2daemon-flexvol:v3.23.2 ${REGISTRY}/pod2daemon-flexvol:v3.23.2
          docker tag coredns/coredns:1.8.0 coredns/coredns:1.8.0
          docker tag kubesphere/k8s-dns-node-cache:1.15.12 ${REGISTRY}/k8s-dns-node-cache:1.15.12
          docker tag kubesphere/kube-apiserver:v1.21.5 ${REGISTRY}/kube-apiserver:v1.21.5
          docker tag kubesphere/kube-controller-manager:v1.21.5 ${REGISTRY}/kube-controller-manager:v1.21.5
          docker tag kubesphere/kube-proxy:v1.21.5 ${REGISTRY}/kube-proxy:v1.21.5
          docker tag kubesphere/kube-scheduler:v1.21.5 ${REGISTRY}/kube-scheduler:v1.21.5
          docker tag kubesphere/pause:3.4.1 ${REGISTRY}/pause:3.4.1
          
          
          docker push ${REGISTRY}/cni:v3.23.2
          docker push ${REGISTRY}/kube-controllers:v3.23.2
          docker push ${REGISTRY}/node:v3.23.2
          docker push ${REGISTRY}/pod2daemon-flexvol:v3.23.2
          docker push ${REGISTRY}/k8s-dns-node-cache:1.15.12
          docker push ${REGISTRY}/kube-apiserver:v1.21.5
          docker push ${REGISTRY}/kube-controller-manager:v1.21.5
          docker push ${REGISTRY}/kube-proxy:v1.21.5
          docker push ${REGISTRY}/kube-scheduler:v1.21.5
          docker push ${REGISTRY}/pause:3.4.1

